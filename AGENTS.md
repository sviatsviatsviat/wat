## Context

**wat** is a Go CLI (`github.com/sviatsviatsviat/wat`, `go 1.26`) built from `cmd/wat`. Current product focus is `wat exec` templating, tests, SemVer/VERSION, Keep a Changelog–style changelog, README, and GitHub Actions; fuller config is deferred.

**Packages:**

- **`internal/core`** — Host-neutral hook types: `HookAdapter`, `HookAdapterFactory`, `HookHandlerProvider`, `HookHandler`, `HookHandlerResult` (see `hook_handler_result.go`).
- **`internal/execcommand`** — The `exec` wat subcommand as [core.HookHandlerProvider] (`NewExecHookHandlerProvider`), flag parsing, `__KEY__` placeholder expansion in command arguments for Cursor (from common + event fields on the hook adapter), PATH/shell subprocess execution (`commandForArgs`, `runSubprocess`). For `-f` / `--file-pattern`, only the **afterFileEdit** / **afterTabFileEdit** exec handler applies the filter when `FILE_PATH` is bound; matching uses Go regexp syntax (not shell globs); invalid regexp sources surface when building the exec provider.
- **`internal/cursor`** — Cursor hook stdin models (`HookDataCommon`, `CursorHookRunData[T]`, event field structs), concrete hook adapter aliases (`DefaultCursorHookAdapter`, `AfterFileEditCursorHookAdapter`, `AfterShellExecutionCursorHookAdapter`, `AfterMCPExecutionCursorHookAdapter`), `HookAdapterBuilder` plumbing, `HookAdapterFactory`, and the per-event adapter registry.

**I/O and execution:** User-facing stderr and hook stdout go through `cli.Console` (`cli.NewConsole` in production; `cli.NewMockConsole` in tests with `StderrBufferWriter` / `StdoutBufferWriter` and `StderrString`, `StdoutString`, etc.). Child hook commands use `Console.ConnectErrorsFrom(*exec.Cmd)` so subprocess stderr shares the diagnostic writer (same stream as `WriteError` / `WriteErrorf`); `internal/execcommand` resolves the subprocess executable with `exec.LookPath` and falls back to the system shell when the first token is not a PATH executable (e.g. Windows shell builtins such as `dir` or `copy`).

**Hook stdin:** `app.Execute` reads hook stdin with `cli.ReadHookStdinJSON` (shared JSON-object validation; whitespace-only input may yield a nil body). Cursor requires a non-empty JSON payload and treats empty or missing JSON as an error; other hosts may allow empty bodies. For Cursor, **`internal/cursor`** orchestrates the path: `HookAdapterFromJSON(eventJSON, console)` unmarshals shared stdin into **`HookDataCommon`** via **`NewHookDataCommon`**, then builds a **`HookAdapter`** whose concrete cursor adapter stores **`CommonInput`** and optional **`EventSpecificInput`** and captures **`cli.Console`** for hook protocol output (**`ReturnEmpty`** writes the default response line). For **`exec`**, **`internal/execcommand`** type-switches on concrete adapters (e.g. **`*cursor.DefaultCursorHookAdapter`**, **`*cursor.AfterFileEditCursorHookAdapter`**, **`*cursor.AfterMCPExecutionCursorHookAdapter`**) and builds placeholder bindings for the template.

**Build and CI:** From the repo root, **`go build ./cmd/wat`** (omit `-o`) so the toolchain writes **`wat.exe`** on Windows and **`wat`** on Unix; gitignored as `/wat` and `*.exe`. Avoid **`go build -o wat`** on Windows — `-o` is literal and can produce a `wat` file without `.exe`. For **local test builds** under `bin/`, use **`go build -o bin/wat.exe ./cmd/wat`** on Windows (include the `.exe` when using `-o`) or **`go build -o bin/wat ./cmd/wat`** on Unix. CI lives under `.github/workflows` (e.g. `ci.yml`).

**Runtime hook pipeline (current design):** Parse program arguments for `wat <host> <subcommand> …` (`parseHost` / `parseSubcommand` in `app`), obtain a host `HookAdapterFactory`, read hook stdin JSON and `HookAdapterFromJSON(…, console)`, then build the subcommand `HookHandlerProvider` from the remaining arguments (e.g. `execcommand.NewExecHookHandlerProvider` parses `exec` flags), then `HookHandlerFor(hook)` (error if the subcommand cannot handle that adapter type), then `Handle()` → `core.HookHandlerResult` (`Code` only). The subcommand `HookHandler` calls `hook.ReturnEmpty()` so the hook writes hook stdout using its captured console (Cursor uses the `{}` + newline protocol). `app.Execute` returns `Code` only. Early failures (unsupported host, stdin JSON parse error, `HookAdapterFromJSON` error, `HookHandlerFor` error) go to stderr only; hook stdout stays empty.

## Rules

- **CLI and help:** Follow Go-style tooling: short description, `Usage:` block, command list. Use raw multiline strings when they keep output clearer.
- **Roadmap, issues, and refactors:** Prefer vertical (end-to-end) functional slices over horizontal, layer-by-layer decomposition when breaking down roadmap or specification work. For architectural refactors the user frames as a clean redesign, breaking CLI or public Go API surface is acceptable; do not keep backward-compatibility shims by default.
- **Changelog and release notes:** In **Added** sections and similar notes, describe what shipped in affirmative, reader-focused terms; avoid negative framing or listing flags and features that were never added. Use **Changed** and **Removed** only when comparing to an **already published** release; while **[Unreleased]** has no prior release (or when correcting the same unreleased entry), update **Added** instead of introducing **Changed**—and use **Removed** only when something is actually dropped versus the last release.
- **Tests:** Assert real behavior; no empty or placeholder tests. When validating the real binary path, prefer building and running `cmd/wat` over only calling internal helpers. For Cursor hook stdout expectations, assert against test-local expected strings (or helper constants owned by tests), not production constants that duplicate the same literal—so protocol changes stay deliberate in both code and tests.
- **Naming:** Use names that match roles (`entrypoint`, `console`, `runSubprocess`, `commandForArgs`, `parseCommandAndCommonParameters`, dedicated factory filenames). Prefer clear identifiers such as `args`, `programArgs`, and `subcommandArgs` over the jargon `argv` in code and documentation unless the Unix/C **argv** sense is explicitly the point. Avoid cryptic short names (`d`, `rest`) and single-letter parameters for important API values. Split growing factory maps/registries into dedicated files (e.g. hook adapter builders beside the factory). For `cli.MockConsole`, name buffers and helpers after the streams (`stdoutBuf`, `StderrString` / `StdoutString`); do not relabel stdout/stderr.
- **Exit codes:** Use named exit-code constants; document exit codes in README in a dedicated list. Keep semantics aligned with the product (e.g. unsupported host name, empty command after templating as bad input where specified).
- **Layering:** Keep host-neutral abstractions in `internal/core`. Put host-specific JSON, payload types, handler factories, and Cursor hook protocol details in each host package (e.g. `internal/cursor`), not in `cli`. Template placeholder binding for **`… exec`** lives in **`internal/execcommand`** (Cursor-shaped input today). Avoid one monolithic stdin envelope for every host. Enforce host-specific stdin rules in that host’s layer (e.g. Cursor non-empty JSON); shared readers alone are not enough.
- **Preserve the pipeline contract:** Do not break the orchestration above: early errors stderr-only and empty hook stdout; successful path emits hook protocol stdout via `adapter.ReturnEmpty()` inside the subcommand `HookHandler`.
- **Host and subcommand arguments:** The first positional argument is the hook host name (e.g. `cursor`); the second is the wat subcommand. The rest is passed into subcommand construction (e.g. `exec` parses `-f` / `--file-pattern` from that slice).
- **Help on errors:** Root help for shared/common parsing failures; subcommands print their own help for subcommand-specific validation (e.g. empty exec template when building the exec command).
- **Wiring:** Prefer explicit factories in `cmd/wat` over blank imports and global registration for `HookAdapterFactory` implementations.
- **API surface:** Keep types, methods, and constructors unexported unless another package needs them. No dead code or exports only for tests if the same behavior can be asserted from package-internal tests. Put small shared utilities in `internal/helpers` instead of duplicating on domain types.
