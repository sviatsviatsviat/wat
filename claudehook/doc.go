// Package claudehook is the Claude Code hook SDK. Hook authors register typed
// handlers; the SDK decodes stdin JSON, dispatches by event, encodes stdout,
// and selects exit codes per the Claude Code hook protocol.
package claudehook
