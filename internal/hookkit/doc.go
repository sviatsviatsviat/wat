// Package hookkit provides shared helpers for per-agent hook SDKs and cmd/wat,
// including codecs, dialect handler bags, the process dialect router, and
// typed handler adapters used by SDK bind code.
package hookkit
